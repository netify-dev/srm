% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_srm.R
\name{sim_srm}
\alias{sim_srm}
\title{Simulate network data from a Social Relations Model}
\usage{
sim_srm(
  n_actors = 10,
  n_time = 1,
  actor_var = 1,
  partner_var = 1,
  unique_var = 1,
  actor_partner_cov = 0,
  relationship_cov = 0,
  grand_mean = 0,
  bipartite = FALSE,
  seed = NULL
)
}
\arguments{
\item{n_actors}{Number of actors (nodes) in the network. For bipartite
networks, a length-2 vector \code{c(n_row, n_col)} specifying the number
of senders and receivers.}

\item{n_time}{Number of time periods (default 1 for cross-sectional).}

\item{actor_var}{Variance of actor (sender) effects.}

\item{partner_var}{Variance of partner (receiver) effects.}

\item{unique_var}{Variance of unique dyadic effects.}

\item{actor_partner_cov}{Covariance between actor and partner effects.
Must satisfy \code{actor_partner_cov^2 <= actor_var * partner_var}.}

\item{relationship_cov}{Covariance of unique effects within dyads
(generalized reciprocity). Must satisfy
\code{abs(relationship_cov) <= unique_var}.}

\item{grand_mean}{Overall network mean.}

\item{bipartite}{If \code{TRUE}, generate a bipartite (two-mode) network.
When bipartite, \code{n_actors} is a length-2 vector \code{c(n_row, n_col)}.
The \code{actor_partner_cov} and \code{relationship_cov} parameters are
ignored for bipartite networks (these quantities are not defined
when senders and receivers are from different populations).}

\item{seed}{Optional random seed for reproducibility.}
}
\value{
A list with components:
\describe{
\item{Y}{A matrix or named list of matrices (if \code{n_time > 1}).}
\item{truth}{A list containing the true \code{actor_effects},
\code{partner_effects}, \code{unique_effects}, and \code{grand_mean}.}
\item{params}{The parameter values used for simulation.}
}
}
\description{
Generates synthetic network matrices from known actor, partner, and unique
effects, useful for teaching, validation, and power analysis.
}
\examples{
# cross-sectional
sim = sim_srm(n_actors = 10, actor_var = 1, partner_var = 0.5,
               unique_var = 2, seed = 6886)
fit = srm(sim$Y)
summary(fit)

\donttest{
# longitudinal
sim_long = sim_srm(n_actors = 8, n_time = 5, seed = 6886)
fit_long = srm(sim_long$Y)
fit_long

# bipartite
sim_bip = sim_srm(n_actors = c(6, 8), bipartite = TRUE, seed = 6886)
fit_bip = srm(sim_bip$Y)
summary(fit_bip)
}

}
